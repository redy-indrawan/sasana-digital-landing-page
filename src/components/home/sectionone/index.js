import React, { Component } from "react";

import { SectionOneComponents } from "./SectionOneComponents";

class SectionOne extends Component {
    render () {
        return (
            <SectionOneComponents />
        )
    }
}

export default SectionOne;